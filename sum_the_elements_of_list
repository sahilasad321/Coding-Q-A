# Sum the elements of list

# Isha is assigned a task to calculate the total amount at the end of the day. Provided with a list, 
# write a Python program for Isha to sum all the elements in the list.

n = input("Enter the list elements separated by spaces :")

li = [float(x) for x in n.split()]
sum_of_list = int(sum(li))

print("The sum of list is :", sum_of_list)
